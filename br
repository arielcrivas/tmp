br () {
	local cmd cmd_file code=0 
	cmd_file=$(mktemp -p /run/user/${EUID} -t .broot-XXXX) 
	if broot --outcmd "$cmd_file" "$argv[@]"
	then
		cmd=$(<"$cmd_file") 
		eval "$cmd"
	else
		code=$status 
	fi
	rm -f "$cmd_file" &> /dev/null
	return $code
}
